// Exemplo de como chamar o painel a partir do ResultSlot (ou outro script).
// Substitua o trecho do seu ResultSlot que fazia HeroPreviewPanel.Instance por algo assim:

// supondo que você tenha um HeroData heroData disponível:
if (HeroPreviewPanel.Instance != null)
{
    HeroPreviewPanel.Instance.Open(heroData); // abre com dados
}
else
{
    Debug.LogWarning("HeroPreviewPanel.Instance é nulo. Verifique se o objeto com HeroPreviewPanel está na cena e ativo.");
}

// Alternativa sem singleton (recomendado se você não quer singletons):
// [SerializeField] private HeroPreviewPanel heroPreviewPanel; // arrastar no inspector
// heroPreviewPanel.Open(heroData);